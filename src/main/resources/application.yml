server: 
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rafliefue/cloudConfigServer       
          default-label: master
          search-paths:
          - '*service'